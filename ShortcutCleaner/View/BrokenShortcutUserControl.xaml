<UserControl x:Class="ShortcutCleaner.View.BrokenShortcutUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShortcutCleaner.View"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="1000"
             Height="25">
    <Grid Height="25">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*" />
            <ColumnDefinition Width="5*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Label x:Name="ShortcutPath" Grid.Column="0"/>
        <Label x:Name="TargetPath" Grid.Column="1"/>
        <Grid Grid.Column="2">
            <Button Margin="0">Delete</Button>
        </Grid>
    </Grid>
</UserControl>
